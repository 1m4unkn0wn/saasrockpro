// @@@ pwned by 1m4unkn0wn @@@
export enum CookieCategory {
  REQUIRED, // userId, lightOrDarkMode, lng, crsf, consent
  FUNCTIONAL, // chat, feedback
  ANALYTICS, // analytics
  ADVERTISEMENT, // ads
}
